HIGH PRIORITY
- fix -i ' ' delimiter
- runnable trait

MID PRORITY
- add and enable extension in extensions.json
- async tasks execute without waiting
- manage installed extensions (delete, update, list)
- multi progress bar
- parallel extension installs
- send multiple extensions in one query in order to avoid multiple requests - avoid spamming mozilla servers
- single client instance

LOW PRIORITY
- &str instead of String
- description language
- fix borrows and ownerships
- fix derives
- fix struct and field access modifiers
- gex configuration file
- if ext list empty, print "no extension found"
- install for multiple browsers
- install for multiple profiles
- log flag
- more browsers and respective data paths
- remove .cloned(), clone(), .to_owned(), .to_string(), .unwrap(), .as_str(), .as_ref()
- remove format!()
- search params (sort output by rating, score...)
- smallvec, tinyvec
- thunderbird (and derivatives) support
- verbose flag
- windows and macos compatibility
